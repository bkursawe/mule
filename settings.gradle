plugins {
    id 'org.gradle.toolchains.foojay-resolver-convention' version '1.0.0'
}
rootProject.name = 'mule'

// engine: rules and computer player; backend: web server and console game
include 'engine', 'backend'
